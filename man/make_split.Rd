% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{make_split}
\alias{make_split}
\title{Deterministic stratified 70/30 train/test split}
\usage{
make_split(y, seed, train_frac = 0.7)
}
\arguments{
\item{y}{Integer vector of binary labels.}

\item{seed}{Integer seed.}

\item{train_frac}{Fraction of cases assigned to train. Default 0.70 to
match the benchmark.}
}
\value{
Logical vector of length `length(y)`; `TRUE` = in train.
}
\description{
Maintains the per-class proportions of `y` in both halves. Reproducible
given `seed`.
}
\examples{
set.seed(1)
y <- rbinom(100, 1, 0.3)
in_tr <- make_split(y, seed = 42)
table(in_tr, y)
}
